import React from 'react'
import ReactDOM from 'react-dom/client'
import WonderOSComplete from './WonderOSComplete.jsx'
import './index.css'  // Optional: Add Tailwind or basic CSS if needed

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WonderOSComplete />
  </React.StrictMode>,
)
